# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`claudecode-duotify` is a newly initialized repository. No build system, language, or framework has been established yet.

## Getting Started

As the project evolves, update this file with:
- Build, lint, and test commands (e.g., `npm run build`, `npm test`, `npm run lint`)
- How to run a single test (e.g., `npm test -- --testNamePattern="test name"`)
- Architecture overview once the codebase takes shape
